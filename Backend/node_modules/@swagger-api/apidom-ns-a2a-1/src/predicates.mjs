import { createPredicate } from '@swagger-api/apidom-core';
import AgentCardElement from "./elements/AgentCard.mjs";
import AgentCapabilitiesElement from "./elements/AgentCapabilities.mjs";
import AgentExtensionElement from "./elements/AgentExtension.mjs";
import AgentProviderElement from "./elements/AgentProvider.mjs";
import AgentInterfaceElement from "./elements/AgentInterface.mjs";
import AgentSkillElement from "./elements/AgentSkill.mjs";
import AgentCardSignatureElement from "./elements/AgentCardSignature.mjs";
import SecurityRequirementElement from "./elements/SecurityRequirement.mjs";
import SecuritySchemeElement from "./elements/SecurityScheme.mjs";
import APIKeySecuritySchemeElement from "./elements/APIKeySecurityScheme.mjs";
import HTTPAuthSecuritySchemeElement from "./elements/HTTPAuthSecurityScheme.mjs";
import MutualTlsSecuritySchemeElement from "./elements/MutualTlsSecurityScheme.mjs";
import OAuth2SecuritySchemeElement from "./elements/OAuth2SecurityScheme.mjs";
import OpenIdConnectSecuritySchemeElement from "./elements/OpenIdConnectSecurityScheme.mjs";
import OAuthFlowsElement from "./elements/OAuthFlows.mjs";
import AuthorizationCodeOAuthFlowElement from "./elements/AuthorizationCodeOAuthFlow.mjs";
import ClientCredentialsOAuthFlowElement from "./elements/ClientCredentialsOAuthFlow.mjs";
import DeviceCodeOAuthFlowElement from "./elements/DeviceCodeOAuthFlow.mjs";
import StringListElement from "./elements/StringList.mjs";
import SkillsElement from "./elements/nces/Skills.mjs";
import SignaturesElement from "./elements/nces/Signatures.mjs";
import SupportedInterfacesElement from "./elements/nces/SupportedInterfaces.mjs";
import SecurityRequirementsElement from "./elements/nces/SecurityRequirements.mjs";
import ExtensionsElement from "./elements/nces/Extensions.mjs";
import SecuritySchemesElement from "./elements/nces/SecuritySchemes.mjs";
/**
 * @public
 */
export const isAgentCardElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AgentCardElement || hasBasicElementProps(element) && isElementType('a2aAgentCard1', element) && primitiveEq('object', element) && hasClass('api', element) && hasClass('agent-card', element);
});

/**
 * @public
 */
export const isAgentCapabilitiesElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AgentCapabilitiesElement || hasBasicElementProps(element) && isElementType('agentCapabilities', element) && primitiveEq('object', element) && hasClass('agent-capabilities', element);
});

/**
 * @public
 */
export const isAgentExtensionElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AgentExtensionElement || hasBasicElementProps(element) && isElementType('agentExtension', element) && primitiveEq('object', element) && hasClass('agent-extension', element);
});

/**
 * @public
 */
export const isAgentProviderElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AgentProviderElement || hasBasicElementProps(element) && isElementType('agentProvider', element) && primitiveEq('object', element) && hasClass('agent-provider', element);
});

/**
 * @public
 */
export const isAgentInterfaceElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AgentInterfaceElement || hasBasicElementProps(element) && isElementType('agentInterface', element) && primitiveEq('object', element) && hasClass('agent-interface', element);
});

/**
 * @public
 */
export const isAgentSkillElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AgentSkillElement || hasBasicElementProps(element) && isElementType('agentSkill', element) && primitiveEq('object', element) && hasClass('agent-skill', element);
});

/**
 * @public
 */
export const isAgentCardSignatureElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AgentCardSignatureElement || hasBasicElementProps(element) && isElementType('agentCardSignature', element) && primitiveEq('object', element) && hasClass('agent-card-signature', element);
});

/**
 * @public
 */
export const isSecurityRequirementElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof SecurityRequirementElement || hasBasicElementProps(element) && isElementType('securityRequirement', element) && primitiveEq('object', element) && hasClass('security-requirement', element);
});

/**
 * @public
 */
export const isSecuritySchemeElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof SecuritySchemeElement || hasBasicElementProps(element) && isElementType('securityScheme', element) && primitiveEq('object', element) && hasClass('security-scheme', element);
});

/**
 * @public
 */
export const isAPIKeySecuritySchemeElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof APIKeySecuritySchemeElement || hasBasicElementProps(element) && isElementType('apiKeySecurityScheme', element) && primitiveEq('object', element) && hasClass('api-key-security-scheme', element);
});

/**
 * @public
 */
export const isHTTPAuthSecuritySchemeElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof HTTPAuthSecuritySchemeElement || hasBasicElementProps(element) && isElementType('httpAuthSecurityScheme', element) && primitiveEq('object', element) && hasClass('http-auth-security-scheme', element);
});

/**
 * @public
 */
export const isMutualTlsSecuritySchemeElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof MutualTlsSecuritySchemeElement || hasBasicElementProps(element) && isElementType('mutualTlsSecurityScheme', element) && primitiveEq('object', element) && hasClass('mutual-tls-security-scheme', element);
});

/**
 * @public
 */
export const isOAuth2SecuritySchemeElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof OAuth2SecuritySchemeElement || hasBasicElementProps(element) && isElementType('oauth2SecurityScheme', element) && primitiveEq('object', element) && hasClass('oauth2-security-scheme', element);
});

/**
 * @public
 */
export const isOpenIdConnectSecuritySchemeElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof OpenIdConnectSecuritySchemeElement || hasBasicElementProps(element) && isElementType('openIdConnectSecurityScheme', element) && primitiveEq('object', element) && hasClass('open-id-connect-security-scheme', element);
});

/**
 * @public
 */
export const isOAuthFlowsElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof OAuthFlowsElement || hasBasicElementProps(element) && isElementType('oauthFlows', element) && primitiveEq('object', element) && hasClass('oauth-flows', element);
});

/**
 * @public
 */
export const isAuthorizationCodeOAuthFlowElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof AuthorizationCodeOAuthFlowElement || hasBasicElementProps(element) && isElementType('authorizationCodeOAuthFlow', element) && primitiveEq('object', element) && hasClass('authorization-code-oauth-flow', element);
});

/**
 * @public
 */
export const isClientCredentialsOAuthFlowElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof ClientCredentialsOAuthFlowElement || hasBasicElementProps(element) && isElementType('clientCredentialsOAuthFlow', element) && primitiveEq('object', element) && hasClass('client-credentials-oauth-flow', element);
});

/**
 * @public
 */
export const isDeviceCodeOAuthFlowElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof DeviceCodeOAuthFlowElement || hasBasicElementProps(element) && isElementType('deviceCodeOAuthFlow', element) && primitiveEq('object', element) && hasClass('device-code-oauth-flow', element);
});

/**
 * @public
 */
export const isStringListElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof StringListElement || hasBasicElementProps(element) && isElementType('stringList', element) && primitiveEq('object', element) && hasClass('string-list', element);
});

/**
 * @public
 */
export const isSkillsElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof SkillsElement || hasBasicElementProps(element) && isElementType('array', element) && primitiveEq('array', element) && hasClass('skills', element);
});

/**
 * @public
 */
export const isSignaturesElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof SignaturesElement || hasBasicElementProps(element) && isElementType('array', element) && primitiveEq('array', element) && hasClass('signatures', element);
});

/**
 * @public
 */
export const isSupportedInterfacesElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof SupportedInterfacesElement || hasBasicElementProps(element) && isElementType('array', element) && primitiveEq('array', element) && hasClass('supported-interfaces', element);
});

/**
 * @public
 */
export const isSecurityRequirementsElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof SecurityRequirementsElement || hasBasicElementProps(element) && isElementType('array', element) && primitiveEq('array', element) && hasClass('security-requirements', element);
});

/**
 * @public
 */
export const isExtensionsElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof ExtensionsElement || hasBasicElementProps(element) && isElementType('array', element) && primitiveEq('array', element) && hasClass('extensions', element);
});

/**
 * @public
 */
export const isSecuritySchemesElement = createPredicate(({
  hasBasicElementProps,
  isElementType,
  primitiveEq,
  hasClass
}) => {
  return element => element instanceof SecuritySchemesElement || hasBasicElementProps(element) && isElementType('object', element) && primitiveEq('object', element) && hasClass('security-schemes', element);
});