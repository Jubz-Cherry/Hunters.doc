import { createNamespace, isStringElement } from '@swagger-api/apidom-core';
import * as a2aPredicates from "../predicates.mjs";
import * as refractorPredicates from "./predicates.mjs";
import a2aNamespace from "../namespace.mjs";
const createToolbox = () => {
  const namespace = createNamespace(a2aNamespace);
  const predicates = {
    ...refractorPredicates,
    ...a2aPredicates,
    isStringElement
  };
  return {
    predicates,
    namespace
  };
};
export default createToolbox;